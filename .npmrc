# Obsidian community releases use bare semver tags (no leading "v"), matching
# manifest.json. Empty the npm version tag prefix so `npm version` creates a
# `0.0.2` tag (not `v0.0.2`) consistent with the release workflow + the store.
tag-version-prefix=""
