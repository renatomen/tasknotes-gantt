---
tags: [task]
status: open
scheduled: 2026-04-01
due: 2026-04-03
effort: Draft
points: 3
workstream:
  - "[[WS Alpha]]"
---

# Task Alpha

A TaskNotes-managed task (the `#task` tag is TaskNotes' default identification),
whose `effort` (text) and `points` (number) columns are inline-editable once the
spec registers them as TaskNotes user fields, whose mapped `status` column
offers the configured-statuses picker, and whose `workstream` (list +
autosuggest filter) column exercises the suggest editor's direct append path.
