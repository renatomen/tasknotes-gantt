// Auto-generated by scripts/generate-release-notes-import.mjs — do not edit manually.
// Regenerated at build time and staged by `npm version`.

export interface ReleaseNoteVersion {
	version: string;
	content: string;
	date: string | null;
	isCurrent: boolean;
}

export const CURRENT_VERSION = "0.1.0-beta.11";
export const RELEASE_NOTES_BUNDLE: ReleaseNoteVersion[] = [
	{
		version: "0.1.0-beta.11",
		content: "# TaskNotes Gantt 0.1.0-beta.11\n\nThe largest release so far, and the first since July. Working-time calendars arrive as a complete feature: calendar notes, a visual editor, working patterns, availability and calendar sets. The chart also gains a legend, independent bar treatment channels, and TaskNotes calendar items in the timeline. And an important fix: task dates no longer shift by a day in timezones behind UTC.\n\nEach entry below is a summary, and its link leads to the full documentation. To try calendars end to end, follow [Add working time: a worked example](https://tngantt.com/getting-started/#add-working-time-a-worked-example).\n\n## Added\n\n- **Calendars and working time.** (#267, #268, #269, #270, #271, #272, #273, #275, #276, #277, #278, #279, #281)\n  - A calendar is an ordinary note in your vault that says which days work happens on. With one in place, the chart can shade the days nobody works and, if you opt in, count working days for an end it works out for you and split bars around days off. All of it is counted in whole days: a calendar can record working hours, but nothing schedules by them yet. [Calendars and working time](https://tngantt.com/features/calendars/)\n  - A task whose Calendar Property link does not resolve schedules as though it had no calendar. [Working days: which dates move](https://tngantt.com/features/calendars/#working-days-which-dates-move)\n  - **Calendar sets** combine several calendar notes into one, so a task can follow all of them through a single link, and the chart can mark the days on which the calendars it shows disagree. The **Select calendars…** command chooses which calendars a view shades. [Calendar sets](https://tngantt.com/features/calendar-sets/)\n  - Working patterns are written as standard iCalendar recurrence rules (RFC 5545 `RRULE` values). A calendar note is still plugin frontmatter: there is no `.ics` import or export. [Recurrence rules, and what they are not](https://tngantt.com/features/calendars/#recurrence-rules-and-what-they-are-not)\n- **A visual editor for calendar notes.** (#286, #287, #288, #289, #290, #291, #292, #293, #294, #295, #296, #297, #298, #300, #301, #303, #304, #305, #306, #307)\n  - Edit a calendar note as a form, with a builder for the working pattern, a timezone picker, a colour picker, and previews of the result before you save. **Create calendar** and **Create calendar set** start a new note. [The calendar editor](https://tngantt.com/features/calendar-editor/)\n  - The editor rewrites each key you change as a whole. Before editing a note you wrote by hand, read [Saving and closing](https://tngantt.com/features/calendar-editor/#saving-and-closing): a comment inside a changed key is lost, and the editor expects the frontmatter written plainly.\n- **Availability blocks: extra working time beyond the main pattern.** (#308, #321)\n  - A calendar can add working days to its pattern, such as an on-call Saturday, as availability blocks. Days off belong in its `non_working` list instead. The editor does not edit blocks yet. [Availability blocks](https://tngantt.com/features/calendar-editor/#availability-blocks)\n- **TaskNotes calendar items in the timeline.** (#386)\n  - The chart can show what the TaskNotes calendar shows, as read-only bars: recurring task instances, time entries, timeblocks, property-based events and external calendars. Each family is opt-in, with one exception the page describes. [Calendar items](https://tngantt.com/features/calendar-items/)\n  - External calendars are read through TaskNotes' own subscriptions. The Gantt adds no feed, credential or fetch of its own, though showing an ICS feed can make TaskNotes fetch it.\n- **A legend that explains the chart.** (#391, #406)\n  - The legend lists the cues the chart can draw, with samples that follow the view you are looking at. It opens at the right or the bottom, and **Default legend position** sets where it opens. [The legend](https://tngantt.com/features/legend/)\n- **Hovering a dependency arrow describes the dependency.** (#404)\n  - Point at the connector itself and the tooltip names the relationship it represents, rather than leaving you to trace the arrow between two rows.\n- **Independent Bar fill, Bar strip and Task icon.** (#312)\n  - **Bar fill** and **Bar strip** are now separate settings, so a bar can be filled by one thing and striped by another, for example filled by status and striped by priority. **Task icon** was already independent and stays so. [Bar fill, Bar strip and Task icon](https://tngantt.com/features/legend/#bar-fill-bar-strip-and-task-icon)\n- **Estimate meaning and Non-working-day rendering are separate settings.** (#315, #319, #320)\n  - **Estimate meaning** chooses whether an end the plugin works out counts the working days of the task's own calendar or plain calendar days, and a single task can override it. **Non-working-day rendering** decides whether days off are only shaded or can also split a bar into segments. [Estimate meaning](https://tngantt.com/settings/timeline/#estimate-meaning)\n- **A prompt when you drag an end the plugin worked out.** (#314, #336, #347, #348)\n  - Resizing a bar's worked-out end can ask whether you meant to grow the estimate or also pin a date there, and a **Don't ask again** toggle saves your answer as the view's **Inferred date drag** setting. The question only arises when **Time Estimate Update** has somewhere to write. [Inferred date drag](https://tngantt.com/settings/timeline/#inferred-date-drag)\n- **A torn edge marks a date the task does not actually have.** (#395, #397, #398, #406)\n  - With **Show date-status indicators on bars** on, the default, a bar end whose date was never written down is cut with a zigzag edge instead of a flat one. A bar too narrow to carry the teeth can hide it. [Date-status indicators](https://tngantt.com/features/appearance/#date-status-indicators)\n\n## Changed\n\n- **Leaving the status, priority or date mappings blank now means \"use the TaskNotes default\", not \"no property\".** (#254)\n  - A blank *Status Property* previously switched off everything that depended on status: colouring bars by status did nothing, the status icon did nothing, and double-clicking a status cell opened the note instead of the picker. (This assumes TaskNotes reports its field configuration; if it cannot, mappings stay blank and the view goes read-only.) Blank mappings now resolve to the properties TaskNotes is configured with, for status and priority as well as dates, and inline editors resolve against the same values.\n\n## Removed\n\n- **The red border on tasks with swapped dates.** (#412)\n  - A task whose start falls after its due date no longer draws a red outline. The orange fill still marks it. A fuller treatment — a badge beside the bar naming what is wrong — is planned; until it ships, a swapped task shown with split rendering carries no visible cue.\n\n## Fixed\n\n- **Tasks no longer shift by a day when you edit their dates.** (#311, #489, #490)\n  - Dates written as plain calendar days were read as UTC instants, so in any timezone behind UTC a task saved as September 21–23 drew on September 20–22 — and dragging it then saved the wrong day, compounding with every correction. Calendar dates are now read as calendar dates.\n  - Three further defects found while verifying that fix are also closed: a moved three-day bar could briefly display as two days, closely spaced corrective moves could roll back after a refresh, and resizing a task that appears in more than one place could leave the other copy behind.\n  - One symptom from the original report — an intermittent change in bar width during a drag — was not reproduced, so #311 stays open for it. Please keep the reports coming if you still see it.\n  - Thanks to @donaldwdci for reporting this and for the detailed follow-up that made the remaining causes findable.\n- **Tooltips never appeared on touch-capable hardware, and dependency tooltips were empty everywhere.** (#404)\n  - Any device reporting a touch point — including most laptops with a touchscreen — suppressed every tooltip in the chart. Tooltips now appear on anything that can hover, a touchscreen laptop included; a touch-only device still has none, since a tooltip is a hover affordance.\n  - Separately, and on every device: the chart hands tooltip content a wrapper around whatever was hovered, and the plugin read it as though it were the task, so dependency tooltips rendered as an empty box. They now carry their content.\n- **Two Gantt views in one workspace no longer interfere with each other.** (#316)\n  - Injected styles were shared across views, so opening a second chart could restyle the first. Each chart now owns its own.\n- **Rows now appear and disappear when their dates change, instead of at the next reopen.** (#472, #473)\n  - With *Show date-status indicators on bars* off, filling in a date a task was missing left *Show tasks with no dates* and *Show tasks with only one date* filtering that row on the classification it used to have — so a task you had just given real dates could stay hidden, and the toolbar could read \"6 results\" over a four-row chart, until you reopened the view.\n  - *Hide top-level subtasks* removes the duplicate top-level copy of a task that also appears as someone's subtask. A task that became a subtask during a refresh kept that duplicate on screen anyway.\n- **Icons on one-cell bars are aligned.** (#393)\n- **A live Bar fill change no longer garbles split rendering.** (#318)\n- **The legend's swatches match the chart.** (#402, #407, #410)\n  - The torn-edge swatch now shows the same cut the chart draws, shading swatches have cells to contrast against, and the legend no longer describes a treatment that was retired.\n- **Calendar editing and validation.** (#299, #302, #323, #324, #326, #327, #328, #329, #330, #331, #333, #334, #335, #337, #338)\n  - Impossible dates are rejected, as are recurrence rules a day-granularity calendar cannot evaluate safely — malformed ones, and ones whose occurrences would drift with whichever window you happen to be looking at — before either can freeze or mis-render the preview. An ordinary open-ended rule, such as a plain Mon–Fri weekly pattern, is still accepted as always.\n  - A colour is sanitised before it reaches an inline style; Discard is disabled while a save is in flight; a newly created calendar note routes to the editor even when the vault indexes it late; deleting a calendar note re-resolves the chart; folder-qualified calendar links resolve; and the conflict banner attributes each conflict to the calendar it came from.\n",
		date: "2026-09-25",
		isCurrent: true,
	},
	{
		version: "0.1.0-beta.10",
		content: "# TaskNotes Gantt 0.1.0-beta.10\n\n## Added\n\n- **A full documentation website — [tngantt.com](https://tngantt.com/).** (#248, #251)\n  - The canonical, feature-by-feature reference for the plugin: getting started, core concepts, a chapter for each feature (with images), and every view option grouped exactly as the plugin groups them. The README is now just a short overview and points here.\n  - It also documents how to *drive* the chart: a new \"Chart controls\" page covers the floating zoom/focus buttons, full screen, and collapse/expand, and every feature now spells out step by step where to find each control and how to trigger it — by click, right-click, or the command palette.\n- **Documentation and source links inside the plugin.** (#250)\n  - The settings tab and the in-app \"What's New\" view now link straight to the documentation website and the GitHub repository, so help is one click away.\n\n## Changed\n\n- **The Bases view is now called \"TaskNotes Gantt\".** (#251)\n  - It previously showed as \"Gantt (OG)\" in the view-type picker; the label now matches the plugin. Existing views keep working — only the display name changed.\n- **The Day scale opens at its most compact day columns.** (#252)\n  - Choosing Day as the default scale used to open with wide day columns, so you had to zoom out by hand every time to reach a workable width. Day now opens at its narrowest day size — the same view you'd otherwise reach by zooming out, and identical on every device. You can still zoom back in with the timeline's **+** button.\n\n## Removed\n\n- **The \"Gantt Task List\" Bases view has been removed.** (#249)\n  - The plugin used to offer a second, list-style view that added little over the timeline and made setup more confusing. The timeline view — now \"TaskNotes Gantt\" — is the single view going forward. If a base still uses the old list view, switch its view type to **TaskNotes Gantt**.\n",
		date: "2026-07-13",
		isCurrent: false,
	},
	{
		version: "0.1.0-beta.9",
		content: "# TaskNotes Gantt 0.1.0-beta.9\n\n## Added\n\n- **Weekend day columns are now shaded in the timeline.** (#241)\n  - Weekend days follow your locale — Saturday/Sunday in most regions, Friday/Saturday in much of the Middle East — and the shading follows the active light/dark theme. It appears at the day and hour zoom levels; coarser scales are unchanged.\n  - Turn it off per view with the new \"Highlight weekends\" toggle under Timeline, on by default. The toggle applies live, keeping your current zoom and scroll position.\n  - ![Weekend day columns shaded in the Gantt timeline at the day scale](https://raw.githubusercontent.com/renatomen/tasknotes-gantt/0.1.0-beta.9/docs/media/weekend-shading.png)\n",
		date: "2026-07-12",
		isCurrent: false,
	},
	{
		version: "0.1.0-beta.8",
		content: "# TaskNotes Gantt 0.1.0-beta.8\n\n## Added\n\n- **Edit task properties inline, right in the grid.** (#224, #225, #226, #227, #228, #231, #232, #233, #234, #236)\n  - Double-click a cell (or press F2) to edit a task without leaving the timeline. Editors are type-aware: text, numbers, checkboxes, dates, status, priority, and multi-value list fields each get the right control.\n  - Dates read and write in your regional locale — so `01/02/2026` is never guessed — and come with a calendar dropdown. Status and priority are pickers fed from the values you've configured in TaskNotes, so only real options are offered.\n  - Text and single-value fields use Obsidian's native `[[` link suggester: start typing `[[` and pick a note exactly as you would in the editor. List fields edit as removable chips, so a multi-value property keeps every `[[wikilink]]` — including aliased links — intact.\n  - Only rows TaskNotes recognizes as tasks are editable, and edits are written through the TaskNotes companion; a standalone (companion-free) timeline stays read-only.\n  - ![A Gantt grid with a task cell open for inline editing](https://raw.githubusercontent.com/renatomen/tasknotes-gantt/0.1.0-beta.8/docs/media/inline-cell-editing-editor-open.png)\n  - ![The locale-aware date editor with its calendar dropdown open on a grid cell](https://raw.githubusercontent.com/renatomen/tasknotes-gantt/0.1.0-beta.8/docs/media/inline-cell-editing-date-editor.png)\n  - ![A status picker open on a grid cell, listing the statuses configured in TaskNotes](https://raw.githubusercontent.com/renatomen/tasknotes-gantt/0.1.0-beta.8/docs/media/inline-cell-editing-status-picker.png)\n\n- **Property cells render as Obsidian markdown.** (#222)\n  - Wikilinks in a property column are now clickable internal links, tag values show as tag pills, and emphasis and lists render — instead of raw `[[...]]` and `#tag` text.\n\n- **Two-way sync between a task's Time Estimate and its bar length.** (#221)\n  - When a task is missing a start or end date, its TaskNotes Time Estimate now sets the bar's length — a per-task override of the view's Default duration. Real dates always win when both are present.\n  - A new per-view \"Time Estimate Update\" mode lets resizing a bar write the new span back to the estimate. Writing needs the TaskNotes companion and is off by default.\n\n- **The grid/timeline split width is now an editable setting.** (#219)\n  - A new \"Table width (px)\" control in a view's Appearance section sets where the grid meets the timeline; leave it empty to fall back to the first column's width. The value now survives tabbing away from the view and back.\n\n- **The view-settings menu is grouped into collapsible sections.** (#217)\n  - The Gantt view's config menu — around two dozen settings — now opens as five collapsible sections (Fields, Progress, Relationships, Timeline, Appearance), so related controls sit together instead of in one long flat list.\n  - ![The Gantt view-settings menu organized into five collapsible sections](https://raw.githubusercontent.com/renatomen/tasknotes-gantt/0.1.0-beta.8/docs/media/view-settings-groups-light.png)\n\n## Changed\n\n- **\"Context bar opacity (%)\" is renamed \"Expanded items opacity (%)\" and moved directly under Expanded relationships.** (#217)\n  - The saved value is unchanged, so existing views keep their setting.\n\n## Fixed\n\n- **Editing gestures no longer trigger an accidental row reorder or parent collapse.** (#234)\n  - Dragging to select text inside a cell editor is no longer read as a row reorder, and a small drag while double-clicking a parent row no longer folds it closed. Drag-to-reorder rows is disabled (it conflicted with editing); expand and collapse now happen only through the chevron.\n\n- **Editing a list cell no longer drops its wikilinks.** (#236)\n  - List fields previously committed their comma-joined display text, which could silently lose an aliased `[[link]]`'s target note. They now edit as chips seeded from the raw frontmatter and commit the real values. In read mode a long list keeps showing links and adds a count badge when it's truncated.\n",
		date: "2026-07-12",
		isCurrent: false,
	},
	{
		version: "0.1.0-beta.7",
		content: "# TaskNotes Gantt 0.1.0-beta.7\n\n## Added\n\n- **Progress on the bars — choose where it comes from, per view.** (#212)\n  - Each Gantt view has a new **Progress mode** setting with two sources:\n    - **TaskNotes Progress** — the bar mirrors the completion TaskNotes shows on its task card (computed from the note's checklist), and updates live as you tick checklist items. It's read-only, so the bar has no drag handle in this mode.\n    - **Property** — the bar reads a numeric 0–100 value from a property you pick in **Progress Property**. Values outside 0–100 are clamped to the nearest end.\n  - In **Property** mode you can **drag the bar's progress handle** to set the value; it saves back to your property on release. Writing requires the TaskNotes companion, and the property must be one TaskNotes recognizes (add it as a custom field in TaskNotes settings). A standalone (companion-free) view still shows the property, just read-only.\n  - Sensible defaults: a companion view defaults to **TaskNotes Progress**; a view that already has a Progress Property mapped keeps reading it (**Property** mode). Progress renders for every task in the chart, including relationship-expanded tasks pulled in from outside the current filter.\n  - ![A Gantt roadmap in a light Obsidian theme showing task bars with progress](https://raw.githubusercontent.com/renatomen/tasknotes-gantt/0.1.0-beta.7/docs/media/gantt-progress-light.png)\n  - ![The same Gantt roadmap in a dark Obsidian theme](https://raw.githubusercontent.com/renatomen/tasknotes-gantt/0.1.0-beta.7/docs/media/gantt-progress-dark.png)\n",
		date: "2026-07-06",
		isCurrent: false,
	},
	{
		version: "0.1.0-beta.6",
		content: "# TaskNotes Gantt 0.1.0-beta.6\n\n## Added\n\n- **Per-view control over bar colors and icons.** (#201)\n  - Each Gantt view now has three independent options: Bar color mode (Fill or a left-edge Strip), Bar color source (Default, By status, By priority, or Obsidian theme), and a Task icon (None, Status, or Priority) shown in a neutral chip on the bar.\n  - Default colors the hierarchy — green parents, blue children. By status / By priority use the colors and icons configured in TaskNotes. Obsidian theme adapts the bars to your current theme (two harmonious accent colors for parents vs children) and re-tints instantly when you switch themes. By status / By priority need the TaskNotes companion; without it they fall back to Default.\n  - The Task icon chip reuses TaskNotes' own status ring / priority dot styling, and shows a colored dot when a value has no configured icon.\n  - For a colorblind-accessible view, pair a color source with the matching Task icon — color alone is not distinguishable for everyone.\n  - ![Two Gantt views in a light theme showing left-edge color strips and on-bar icon chips: one view striped by priority with status icons, the other striped by status with priority icons](https://raw.githubusercontent.com/renatomen/tasknotes-gantt/0.1.0-beta.6/docs/media/bar-treatments-light.png)\n  - ![The same two Gantt views in a dark theme, showing the strips and icon chips adapt to the active Obsidian theme](https://raw.githubusercontent.com/renatomen/tasknotes-gantt/0.1.0-beta.6/docs/media/bar-treatments-dark.png)\n\n## Changed\n\n- **Bars now color the hierarchy by default (green parents, blue children) instead of by TaskNotes status.** (#201)\n  - To color by status again, set a view's Bar color source to \"By status\" (mode \"Fill\").\n\n## Fixed\n\n- **Dark-mode expand/collapse chevrons and panel-resizer arrows are now visible, themed, and aligned.** (#207)\n  - The grid's parent expand/collapse chevrons were painted near-black on dark chart surfaces, so they were almost invisible regardless of the theme; they now follow your theme's text color and lift to the accent color on hover. The split-panel resizer arrows got the same treatment, and leaf-row labels now line up with parent-row labels instead of sitting a few pixels to the right.\n- **Single-parent nested tasks are no longer wrongly marked as replicated.** (#204)\n  - With \"Hide top-level subtasks\" on, a task shown exactly once could still be drawn with the diagonal \"replicated\" hatch as if it appeared in several places. The hatch now counts only the placements actually visible, so it shows up only for tasks genuinely displayed more than once.\n- **A completed status with no configured icon now shows a filled disc, matching TaskNotes.** (#210)\n  - The on-bar icon chip drew a hollow ring for such statuses; TaskNotes fills that dot into a solid disc when the status is flagged completed in its settings, so the two views now agree. Statuses with a configured icon, priority dots, and non-completed statuses are unchanged.\n",
		date: "2026-07-03",
		isCurrent: false,
	},
	{
		version: "0.1.0-beta.5",
		content: "# TaskNotes Gantt 0.1.0-beta.5\n\n## Changed\n\n- **The in-app \"What's New\" view has a new TaskNotes-style design.** (#197)\n  - Each release now appears as its own collapsible card, with a prominent version number, a human-readable date, a chevron that rotates as you open it, and a filled \"Current\" pill marking the release you're on — matching the look of TaskNotes' own release-notes screen.\n  - The styling is fully theme-adaptive: the cards, pills, and chevron all follow your Obsidian theme's colours and re-resolve instantly when you switch between light and dark, so nothing looks out of place.\n  - Past beta releases are now included in the view, so the first time you open it after updating you get real, scrollable release history rather than an empty screen.\n  - ![The redesigned What's New view in a dark theme — each release is a collapsible card with a version, date, \"Current\" pill, and chevron, expanding and collapsing](https://raw.githubusercontent.com/renatomen/tasknotes-gantt/0.1.0-beta.5/docs/media/whats-new-screen-dark.gif)\n  - ![The same What's New view in a light theme, showing the cards adapt to the active Obsidian theme](https://raw.githubusercontent.com/renatomen/tasknotes-gantt/0.1.0-beta.5/docs/media/whats-new-screen-light.gif)\n",
		date: "2026-07-01",
		isCurrent: false,
	},
	{
		version: "0.1.0-beta.4",
		content: "# TaskNotes Gantt 0.1.0-beta.4\n\n## Fixed\n\n- **Full screen no longer hides Obsidian popups.** (#191)\n  - Previously, opening the Gantt in full screen pushed Obsidian's own popups behind it — double-clicking a bar to open the TaskNotes edit modal appeared to do nothing, and the command palette and context menus were unreachable without un-maximizing first. Full screen now maximizes the view *within* Obsidian, so the edit modal, command palette, context menus, and hover previews all layer above the chart as expected. The \"Full screen\" label and icon are unchanged.\n  - ![Full screen maximizes within Obsidian; the command palette stays visible above the maximized chart](https://raw.githubusercontent.com/renatomen/tasknotes-gantt/0.1.0-beta.4/docs/media/fullscreen-popup-visibility.gif)\n",
		date: "2026-07-01",
		isCurrent: false,
	},
	{
		version: "0.1.0-beta.3",
		content: "# TaskNotes Gantt 0.1.0-beta.3\n\n## Added\n\n#### Focus on a specific task\n\n- A new crosshair button in the chart controls — and a \"Gantt: Focus on task…\" command — opens a fuzzy search over every task in the chart (both the tasks matched by your Base and any pulled in through relationships).\n- Pick one and the Gantt brings it into view for you: it expands only the parents needed to reveal it, zooms so the whole bar is visible without filling the chart, scrolls to it on both axes, and highlights it.\n- Tasks without a full date range (milestones, unscheduled) are revealed at the current zoom. This is navigation only — focusing never opens or edits the task. (#189)\n\n![Focus on a task — search for a task, expand its collapsed parent, then zoom, scroll, and highlight it](https://raw.githubusercontent.com/renatomen/tasknotes-gantt/0.1.0-beta.3/docs/releases/assets/0.1.0-beta.3-focus.gif)\n\n## Changed\n\n#### Clicking a task bar now selects it first, instead of immediately opening it\n\n- A single click (or tap) on an unselected bar just selects and highlights its row, so you can pick out a task without the edit modal popping up.\n- The configured TaskNotes click action runs when you click an already-selected bar, or on a double-click — `singleClickAction` / `doubleClickAction` are honoured exactly as before.\n- Ctrl/Cmd-click still opens the note in a new tab, and the behaviour works the same on touch (tap to select, tap again or double-tap to open, long-press for the task menu). (#188)\n",
		date: "2026-06-30",
		isCurrent: false,
	},
	{
		version: "0.1.0-beta.2",
		content: "# TaskNotes Gantt 0.1.0-beta.2\n\nThis beta focuses on render stability for large and changing result sets, and\nintroduces an in-app \"What's New\" view so updates are visible without leaving\nObsidian.\n\n## Added\n\n- In-app **\"What's New\"** view. After updating the plugin, the release notes for\n  the new version open automatically once; you can reopen them any time with the\n  **\"Show release notes\"** command. Versions are listed newest-first and each is\n  collapsible, and a settings toggle lets you turn off the auto-open on update.\n  (#157)\n\n## Fixed\n\n- **Fixed a long freeze (up to roughly 25 seconds) when clearing a Bases search** —\n  or otherwise swinging a large set of tasks out of and back into the view. Large\n  changes now re-seed the timeline in a single pass instead of applying thousands\n  of per-row updates, cutting the work by about 99.8% on large vaults. (#161)\n- **Fixed a render loop that could be triggered by changing the result set** —\n  clearing a search, changing a filter, or toggling a view display option. View\n  display options no longer feed back into how the task set is derived. (#161)\n- **Fixed dependency edges and the **Show all / Inherit** dependency modes** failing\n  to appear when the TaskNotes relationship index was still warming up as the view\n  mounted. The view now re-checks for a bounded window and self-heals once\n  TaskNotes relationships are ready, so dependencies show up without a manual\n  reload. (#161, #167)\n",
		date: "2026-06-28",
		isCurrent: false,
	},
	{
		version: "0.1.0-beta.1",
		content: "# TaskNotes Gantt 0.1.0-beta.1\n\nFirst public beta. TaskNotes Gantt adds an interactive **Gantt timeline** to Obsidian\nas a Bases view: point it at your TaskNotes tasks (or any Base) and get a live schedule\nwith dependency arrows, drag-to-reschedule, and parent/child roll-up — edits written\nstraight back to your task notes through TaskNotes.\n\nThis is an early beta — expect rough edges, and please\n[report anything that looks off](https://github.com/renatomen/tasknotes-gantt/issues).\n\n## Added\n\n**Scheduling**\n- Task bars from each task's start → end dates (TaskNotes `scheduled` → `due` by default,\n  or any date properties you map).\n- Drag to reschedule and resize to change duration — persisted back to the task note.\n- Parent/child roll-up: subtasks nest under their parent, and dragging a parent moves its\n  whole subtree. Choose whether parent dates update automatically, ask first, or never.\n\n**Dependencies**\n- Renders all four RFC 9253 relationship types — Finish-to-Start, Finish-to-Finish,\n  Start-to-Start, Start-to-Finish — plus gap/lag, from TaskNotes' `blockedBy`.\n- Create and delete Finish-to-Start dependencies by dragging between bars.\n- Choose how arrows draw across duplicated multi-parent instances.\n\n**Task editing**\n- Click a bar to open the note or TaskNotes' edit modal (honoring your single-/double-click\n  settings; ⌘/Ctrl-click opens it in a new tab).\n- Right-click for TaskNotes' full task context menu — status, priority, contexts, tags, dates.\n- Bars are colored by your configured TaskNotes statuses.\n\n**Structure & display**\n- Expand relationships beyond the Base filter (\"Show all\") to pull in related subtasks for\n  context (with adjustable context-bar opacity); optionally hide top-level subtasks.\n- Sort the grid by any column.\n- Zoom the timeline by hours, days, weeks, or months.\n- Resizable grid/timeline split that remembers its width.\n- Full-screen mode.\n- Optional toolbar with an Auto/Light/Dark theme switch.\n- Configurable min/max height (fits content, then scrolls internally).\n- Control how tasks with missing or partial dates appear, with on-bar date-status indicators.\n\n**Standalone (read-only, no TaskNotes)**\n- A read-only timeline over any Obsidian Base — map start/end properties and your notes\n  render as bars. (No write-back, dependency arrows, or status colors in this mode.)\n\n**Privacy**\n- Runs entirely on your device: no network requests, no telemetry, no remote code. Release\n  builds carry a verifiable GitHub build-provenance attestation.\n\n## Known limitations\n- Only **Finish-to-Start** dependencies can be authored from the chart today; the other three\n  types render but are edited in TaskNotes. Editing relationship type and gap/lag from the\n  chart is in progress.\n- Standalone (no-TaskNotes) mode is read-only.\n- Early beta — expect rough edges.\n\n## Requirements\n- Obsidian 1.10.0+ with the core **Bases** plugin enabled.\n- **TaskNotes** for the full read/write experience (optional for the read-only timeline).\n\n## Acknowledgements\n- Built on [SVAR Svelte Gantt](https://svar.dev/svelte/gantt/); pairs with\n  [TaskNotes](https://github.com/callumalpass/tasknotes) by @callumalpass.\n",
		date: "2026-06-23",
		isCurrent: false,
	}
];
