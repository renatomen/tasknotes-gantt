/**
 * Barrel for the scheduling layer.
 *
 * Populated by U10 (SchedulingEngine: Tier-1 roll-up + cascade).
 *
 * @module scheduling
 */

// Placeholder barrel — re-exports land here when U10's SchedulingEngine ships.
// `export type` keeps this a module (not a global script) without an empty
// `export {}` specifier list.
export type SchedulingBarrelPlaceholder = never;
